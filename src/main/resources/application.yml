app:
  jwt:
    # 256位安全密钥 (32字节)
    secret: MySecureJwtSecretKeyForSocialNetworkDemo2025WithMinimum256BitsLength
    expirationMs: 3600000  # 1小时 (毫秒)

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/social_network?characterEncoding=UTF-8&useUnicode=true&useSSL=false&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
