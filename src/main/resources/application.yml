app:
  jwt:
    secret: LiveChatRoom
    expirationMs: 360000

spring:
  datasource:
    url: jdbc:mysql://192.168.36.134:3306/novel?useSSL=false&serverTimezone=UTC
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis-plus:
  mapper-locations: classpath:/mapper/*.xml
  configuration:
    map-underscore-to-camel-case: true